[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = *Tests
addopts = -ra
markers =
    unit: unit tests that check isolated functions and classes
    integration: integration tests that check multiple modules together
    e2e: end-to-end browser tests using Selenium
    slow: slow tests that may take longer to execute